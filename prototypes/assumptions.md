# 미검증 가정

목적: 검증 안 된 추측을 격리합니다. 추측은 본문 문서에 들어가지 않습니다(하네스 원칙 5). 버리지 않고 이 파일에서 대기시킵니다. 작성자는 사용자(author-only)입니다.

형식:

    <가정 1줄> — 미검증, <확인 방법/시점>

해소 꼬리표:

- 이 파일은 append-only입니다.
- 판정이 나면 해당 줄 끝에만 ` → 검증됨 (<cycle-NN 또는 VS 측정>)` 또는 ` → 반증됨 (killed-hypotheses YYYY-MM-DD)` 를 붙입니다.
- 위 꼬리표 추가 외에는 기존 줄을 수정하지 않습니다.

점검 시점:

1. 사이클 계획 시: 다음 가설 후보로 훑습니다.
2. Stage 3 진입 시: 측정으로 풀릴 것을 확인합니다.
3. Stage 4 기록 시: 본문 승격 대상을 확인합니다.

규칙:

- 영영 해소 안 되는 추측도 허용합니다.
- 목적은 전수 검증이 아니라 추측이 사실처럼 본문에 들어가는 것을 막는 일입니다.
- 새 가정은 아래 주석 밑에 한 줄씩 추가합니다.

---

<!-- 미검증 가정 추가 시작 -->
